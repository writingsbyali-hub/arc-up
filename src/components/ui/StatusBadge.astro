---
interface Props {
  status: 'ready-to-build' | 'coming-soon' | 'help-shape' | 'active' | 'in-development' | 'future-work';
  className?: string;
  size?: 'sm' | 'md' | 'lg';
}

const {
  status,
  className = '',
  size = 'md'
} = Astro.props;

const statusConfig = {
  'ready-to-build': {
    label: 'Ready to Build',
    color: 'bg-blue-500/20 text-blue-400 border-blue-500/40',
    icon: '📐'
  },
  'coming-soon': {
    label: 'Coming Soon',
    color: 'bg-purple-500/20 text-purple-400 border-purple-500/40',
    icon: '🔜'
  },
  'help-shape': {
    label: 'Help Shape This',
    color: 'bg-yellow-500/20 text-yellow-400 border-yellow-500/40',
    icon: '🤝'
  },
  'active': {
    label: 'Active',
    color: 'bg-green-500/20 text-green-400 border-green-500/40',
    icon: '✓'
  },
  'in-development': {
    label: 'In Development',
    color: 'bg-orange-500/20 text-orange-400 border-orange-500/40',
    icon: '🔧'
  },
  'future-work': {
    label: 'Future Work',
    color: 'bg-gray-500/20 text-gray-400 border-gray-500/40',
    icon: '💡'
  }
};

const sizeClasses = {
  sm: 'text-xs px-2 py-1 gap-1',
  md: 'text-sm px-3 py-1.5 gap-1.5',
  lg: 'text-base px-4 py-2 gap-2'
};

const config = statusConfig[status];
---

<span
  class={`inline-flex items-center font-medium rounded-full border ${config.color} ${sizeClasses[size]} ${className}`}
>
  <span class="text-sm">{config.icon}</span>
  <span>{config.label}</span>
</span>

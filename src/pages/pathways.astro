---
// ====================================================================
// PATHWAYS HUB PAGE
// Gateway to the three pillars: Lab, Framework, Commons
// ====================================================================
import Layout from '~/layouts/PageLayout.astro';

const metadata = {
  title: 'Pathways — Explore Our Work',
};

const pillars = [
  {
    slug: 'lab',
    title: 'Lab',
    tagline: 'Building & Testing',
    description: 'Design, build, and test plasma systems. From hardware prototypes to experimental protocols—this is where ideas become tangible technology.',
    streams: [
      { name: 'Physical Plasma Systems', slug: 'physical', status: 'Active' },
      { name: 'Plasma-Water Interface', slug: 'water', status: 'Active' },
      { name: 'Ecosystem Dynamics', slug: 'ecosystem', status: 'Active' }
    ],
    color: 'yellow',
    bgGradient: 'from-yellow-500/20 via-yellow-500/5 to-transparent',
    borderColor: 'border-yellow-500/30',
    hoverBorder: 'hover:border-yellow-400',
    textColor: 'text-yellow-400',
    buttonBg: 'bg-yellow-500 hover:bg-yellow-400',
    buttonText: 'text-gray-900'
  },
  {
    slug: 'framework',
    title: 'Framework',
    tagline: 'Protocols & Governance',
    description: 'Develop the methods, standards, and governance structures that ensure research remains open, safe, and reproducible across contexts.',
    streams: [
      { name: 'Protocols', slug: 'protocols', status: 'Forming' },
      { name: 'Governance', slug: 'governance', status: 'Forming' },
      { name: 'Validation', slug: 'validation', status: 'Forming' }
    ],
    color: 'green',
    bgGradient: 'from-green-400/20 via-green-400/5 to-transparent',
    borderColor: 'border-green-400/30',
    hoverBorder: 'hover:border-green-400',
    textColor: 'text-green-400',
    buttonBg: 'bg-green-400 hover:bg-green-300',
    buttonText: 'text-gray-900'
  },
  {
    slug: 'commons',
    title: 'Commons',
    tagline: 'Data & Knowledge',
    description: 'Share data, visualizations, and models. Build the collective knowledge infrastructure for open plasma research and ecological regeneration.',
    streams: [
      { name: 'Data', slug: 'data', status: 'Forming' },
      { name: 'Visualization', slug: 'visualization', status: 'Forming' },
      { name: 'Modeling', slug: 'modeling', status: 'Forming' }
    ],
    color: 'blue',
    bgGradient: 'from-blue-400/20 via-blue-400/5 to-transparent',
    borderColor: 'border-blue-400/30',
    hoverBorder: 'hover:border-blue-400',
    textColor: 'text-blue-400',
    buttonBg: 'bg-blue-400 hover:bg-blue-300',
    buttonText: 'text-gray-900'
  }
];
---

<Layout metadata={metadata}>
  <!-- Hero Section with Gradient -->
  <section class="relative py-16 md:py-24 bg-[radial-gradient(circle_at_top,#181f3d_0%,#080E21_90%)]">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 text-center">
      <p class="text-sm font-mono text-arc-electric uppercase tracking-widest mb-4">
        Three Pillars of Open Research
      </p>
      <h1 class="text-5xl md:text-6xl font-mono mb-6 tracking-tight text-white">
        Pathways
      </h1>
      <p class="text-xl text-gray-300 max-w-3xl mx-auto leading-relaxed">
        Choose your entry point. Each pillar connects to streams of work—from building plasma systems to governing shared knowledge. All paths lead to the same commons of repair.
      </p>
    </div>
  </section>

  <!-- Main Content -->
  <section class="bg-primary-900 py-12 md:py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">

      <!-- Intro Text -->
      <div class="max-w-3xl mx-auto text-center mb-16 space-y-4">
        <p class="text-xl text-gray-300 leading-relaxed">
          Each pillar represents a distinct approach to contributing. Within each pillar are <strong class="text-white">streams</strong>—specific areas of focus containing projects, research questions, and opportunities for collaboration.
        </p>
        <p class="text-base text-gray-400">
          Click any pillar to explore its streams and begin contributing.
        </p>
      </div>

      <!-- Pillars Grid -->
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mb-16">
        {pillars.map((pillar) => (
          <a
            href={`/${pillar.slug}`}
            class={`group relative bg-gradient-to-br ${pillar.bgGradient} border ${pillar.borderColor} ${pillar.hoverBorder} rounded-2xl p-8 backdrop-blur-sm transition-all duration-300 hover:scale-[1.03] hover:shadow-2xl hover:shadow-${pillar.color}-400/20 block`}
          >
            <!-- Pillar Header -->
            <div class="space-y-4 mb-8">
              <div class="flex items-center justify-between">
                <h2 class={`text-4xl font-bold font-mono ${pillar.textColor}`}>
                  {pillar.title}
                </h2>
                <svg class={`w-8 h-8 ${pillar.textColor} transform group-hover:translate-x-1 transition-transform`} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                </svg>
              </div>
              <p class={`text-sm font-mono uppercase tracking-wider ${pillar.textColor} opacity-80`}>
                {pillar.tagline}
              </p>
              <p class="text-gray-300 leading-relaxed">
                {pillar.description}
              </p>
            </div>

            <!-- Streams List -->
            <div class="space-y-3 mb-8">
              <p class="text-xs font-mono uppercase tracking-wider text-gray-500">
                Active Streams
              </p>
              {pillar.streams.map((stream) => (
                <div class="flex items-center justify-between py-2 px-3 bg-gray-900/30 rounded-lg border border-gray-700/30">
                  <span class="text-sm text-gray-300">{stream.name}</span>
                  <span class={`text-xs font-mono px-2 py-1 rounded ${stream.status === 'Active' ? 'bg-green-500/20 text-green-400' : 'bg-yellow-500/20 text-yellow-400'}`}>
                    {stream.status}
                  </span>
                </div>
              ))}
            </div>

            <!-- Explore Button -->
            <div class={`${pillar.buttonBg} ${pillar.buttonText} text-center py-3 px-6 rounded-lg font-mono font-bold transition-all duration-300 group-hover:scale-[1.05]`}>
              Explore {pillar.title} →
            </div>
          </a>
        ))}
      </div>

      <!-- Bottom Info Section -->
      <div class="max-w-4xl mx-auto">
        <!-- How to Navigate -->
        <div class="bg-gray-900/30 border border-gray-700/50 rounded-2xl p-8 backdrop-blur-sm mb-8">
          <h3 class="text-2xl font-bold text-white mb-4 font-mono">How to Navigate Pathways</h3>
          <div class="space-y-4 text-gray-300">
            <div class="flex gap-4">
              <span class="text-2xl">01</span>
              <div>
                <p class="font-semibold text-white mb-1">Choose a Pillar</p>
                <p>Lab (build), Framework (govern), or Commons (share)</p>
              </div>
            </div>
            <div class="flex gap-4">
              <span class="text-2xl">02</span>
              <div>
                <p class="font-semibold text-white mb-1">Explore Streams</p>
                <p>Each pillar contains 3 streams with specific focus areas</p>
              </div>
            </div>
            <div class="flex gap-4">
              <span class="text-2xl">03</span>
              <div>
                <p class="font-semibold text-white mb-1">Find Projects</p>
                <p>Within streams, discover projects, research questions, and references</p>
              </div>
            </div>
            <div class="flex gap-4">
              <span class="text-2xl">04</span>
              <div>
                <p class="font-semibold text-white mb-1">Get Involved</p>
                <p>Ready to contribute? Visit <a href="/get-started" class="text-living-earth hover:text-arc-electric transition underline">Get Started</a> to begin your journey</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Not Sure Where to Start? -->
        <div class="text-center p-8 bg-arc-electric/5 border border-arc-electric/30 rounded-2xl backdrop-blur-sm">
          <h3 class="text-xl font-bold text-white mb-3 font-mono">Not sure where to start?</h3>
          <p class="text-gray-300 mb-6">
            Learn about our research approach first, or jump straight into getting started.
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href="/research"
              class="px-6 py-3 bg-gray-800 hover:bg-gray-700 text-white rounded-lg font-mono transition-all duration-300 hover:scale-[1.05]"
            >
              ← Read About Research
            </a>
            <a
              href="/get-started"
              class="px-6 py-3 bg-arc-electric hover:bg-arc-electric/90 text-white rounded-lg font-mono transition-all duration-300 hover:scale-[1.05]"
            >
              Get Started →
            </a>
          </div>
        </div>
      </div>

    </div>
  </section>
</Layout>
